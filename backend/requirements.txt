# Shared runtime dependencies for the CAD backend and the CAD CLI package.
#
# The CLI package lives under `agent-harness/cli_anything/cad/`, but it imports
# backend modules directly, so this dependency set covers both entry points.

# Core web stack
# Starlette 1.x switches TestClient to httpx2. Keep the supported 0.x stack
# until the application migrates its test client.
fastapi>=0.115.0,<0.141.0
starlette>=0.40.0,<1.0.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# Async jobs / storage
celery>=5.4.0
redis>=5.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0

# CAD / translation processing
ezdxf>=1.3.0
pandas>=2.2.0
openpyxl>=3.1.0
requests>=2.31.0

# Configuration / validation / logging
pydantic>=2.8.0
pydantic-settings>=2.4.0
structlog>=24.1.0
python-dotenv>=1.0.1
rich>=13.7.0
psutil>=5.9.0

# Security helpers kept by the backend
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# CLI / local release support used by the installable package
build>=1.2.0
setuptools>=68.0
wheel>=0.43.0
click>=8.1.0
prompt-toolkit>=3.0.0

# Windows-only CAD integration
pywin32>=306; platform_system == "Windows"

# Development / verification
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0,<0.29.0
